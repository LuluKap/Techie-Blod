<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Techh Blog</title>
        <link rel="stylesheet" href="./public/css/style.css">
    </head>

    <body>
        <div class="container">
            <header>
                <h1>
                    <a href="/">Techh Blog</a>
                </h1>
                <nav>
                    {{#if loggedIn}}
                        <a href="/dashboard">Dashboard</a>
                        <a href="/logout">Logout</a>
                    {{else}}
                        <a href="/login">Login</a>
                        <a href="/signup">Signup</a>
                    {{/if}}
                </nav>
            </header>
            <main>
                {{{ body }}} <!-- This is where the content will be rendered -->
            </main>
            <footer>
                <p>Created by <a href="https://github.com/LuluKap">Lulu K</a></p>
            </footer>
        </div>
        {{#if loggedIn}}
            <script>
                window.user = {{user}}
            </script>
        {{/if}}
    </body>
</html>